import { IPuddleApp } from "puddle-interfaces/IPuddleApp";

export function ApplyPlugins(currentPuddle : IPuddleApp){
    /* Plugins go here to add handlers */
}

export function SetupPuddle(currentPuddle : IPuddleApp){
    /* The data store and message queue should be configured here */
}